import { Raleway, Anonymous_Pro } from 'next/font/google';

export const raleway = Raleway({ subsets: ['latin'], weight: ['400'] });
export const anon_pro = Anonymous_Pro({ subsets: ['latin'], weight: ['400'] });
